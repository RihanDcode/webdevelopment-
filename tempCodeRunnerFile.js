// const express = require('express');
// const app = express();
// const port = 3000;
// app.get('/', (req, res) => {
//   res.send('Hello World! This is Danish Khan from India');
// });

// app.listen(port, () => {
//   console.log(`Example app listening on port ${port}`);
// });                                    -+

l/*et arr1 = [100,200,300,400];
let arr2 = [100,200,300,400,];

if(arr1.length[0] == arr2.length[0] && arr1.length[1] == arr2.length[1] && arr1.length[2] == arr2.length[2] && arr1.length[3] == arr2.length[3]){
  console.log("arrays are equal");
}else{
  console.log("arrays are not equal");
}*/

/*const arr1 = [100, 200, 300, 400];
const arr2 = [100, 200, 300, 400];

// Step 1: length check
if(arr1.length !== arr2.length) {
  console.log("Arrays not same");
} else {
  // Step 2: har element compare using every()
  const isSame = arr1.every((value, index) => value === arr2[index]);

  if (isSame) {
    console.log("Arrays are same");
  } else {
    console.log("Arrays are different");
  }
}*/

