<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %>
    </title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 20px;
            background-color: #f4f7f6;
        }

        .dashboard {
            background: #fff;
            padding: 30px;
            border-radius: 8px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            max-width: 900px;
            margin: 30px auto;
        }

        .header h1 {
            color: #333;
            border-bottom: 2px solid #eee;
            padding-bottom: 10px;
        }

        .info-grid {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 20px;
            margin-top: 20px;
        }

        .card {
            background: #e9f0ff;
            padding: 15px;
            border-radius: 6px;
            text-align: center;
        }

        .card h3 {
            margin-top: 0;
            color: #0056b3;
        }

        .attendance-chart {
            margin-top: 30px;
            padding: 20px;
            border: 1px solid #ddd;
            border-radius: 6px;
        }
    </style>
</head>

<body>
    <div class="dashboard">
        <div class="header">
            <h1>👋 Welcome, <%= user.name %>!</h1>
            <p><strong>Roll No:</strong>
                <%= user.rollNumber %>
            </p>
            <p><strong>Email:</strong>
                <%= user.email %>
            </p>
            <a href="/auth/logout">Logout</a>
        </div>

        <hr>

        <h2>📚 Basic Info & Courses</h2>
        <div class="info-grid">
            <div class="card">
                <h3>Total Courses</h3>
                <p><strong>
                        <%= user.courses.length %>
                    </strong></p>
            </div>
            <div class="card">
                <h3>Current GPA</h3>
                <p><strong>
                        <%= user.gpa || 'N/A' %>
                    </strong></p>
            </div>
            <div class="card">
                <h3>Routine Status</h3>
                <p><strong>Updated</strong></p>
            </div>
        </div>

        <hr>

        <h2>📊 Attendance Overview</h2>
        <div class="attendance-chart">
            <p><strong>Overall Attendance:</strong>
                <% const totalClasses=user.attendance.totalClasses || 1; const attended=user.attendance.attended || 0;
                    const percentage=((attended / totalClasses) * 100).toFixed(2); %>
                    <span style="color: <%= percentage < 75 ? 'red' : 'green' %>;">
                        <%= percentage %>%
                    </span>
            </p>

            <p>Detailed Course Attendance will be displayed here.</p>
        </div>

        <h2>🗓️ Today's Routine</h2>
        <p>You can display the timetable/routine data here.</p>
    </div>
</body>

</html>